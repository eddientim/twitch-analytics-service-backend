language: java
jdk:
  - openjdk11
branches:
  only:
  - feat/*
  - main

# whitelist
jobs:
  include:
    - stage: Maven Clean
      script: mvn clean
    - stage: Maven Install
      script: mvn clean
    - stage: Unit Tests
      script: mvn test
    - stage: Checkstyle Validation
      script: mvn validate

